namespace FirstWebMVC.Models.Process

{
    public enum SystemPermissions
    {
        ViewStudent, CreateStudent, EditStudent, DeleteStudent,
        ViewMemberUnit, CreateMemberUnit, EditMemberUnit, DeleteMemberUnit,
        ViewRole, CreateRole, EditRole, DeleteRole, AssignClaim,
        ViewAccount, AssignRole, AddClaim, DeleteClaim
    }

}